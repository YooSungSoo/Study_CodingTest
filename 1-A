// 2309번 일곱 난쟁이 
// next_permutation 순열 접근

#include<bits/stdc++.h>

using namespace std;

int main(void){
	
	int a[9], b[7]; 
	
	for(int i=0;i<9;i++){
	
	cin >> a[i];
	}

	sort(a,a+9);

	do{
		int sum = 0;
		for(int i=0;i<7;i++) sum += a[i];
		
		if(sum==0) break;
	}while(next_permutation(a,a+9));

	for(int i =0;i<7;i++){
		cout << a[i] << endl;
	}
	return 0;
}
